import { DocLayout } from "@/layouts/DocLayout";

import Main from "./demos/Main";
import mainSourceCode from "./demos/Main.source-code.codegen";
import DelayedTooltipOnHover from "./demos/DelayedTooltipOnHover";
import delayedTooltipOnHoverSourceCode from "./demos/DelayedTooltipOnHover.source-code.codegen";

<DocLayout.Live node={<Main />} code={mainSourceCode["index.tsx"]} />

## Dependency Graph

<DocLayout.DepGraph
  nodes={[
    { id: "tooltip", label: "Tooltip" },
    { id: "copy-ui-provider", label: "CopyUiProvider" },
    { id: "popover", label: "Popover" },
    { id: "classnames", label: "classnames", color: "green" },
  ]}
  edges={[
    { from: "tooltip", to: "popover" },
    { from: "tooltip", to: "copy-ui-provider" },
    { from: "tooltip", to: "classnames" },
  ]}
/>

## Examples

### Delayed Tooltip on Hover

<DocLayout.Live
  node={<DelayedTooltipOnHover />}
  code={delayedTooltipOnHoverSourceCode["index.tsx"]}
/>
