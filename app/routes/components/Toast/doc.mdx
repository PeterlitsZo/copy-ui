import { DocLayout } from "@/layouts/DocLayout";
import { Typography } from "@/components/Typography";

import Demo01 from "./demos/Demo01";
import demo01SourceCode from "./demos/Demo01.source_code.codegen";

<DocLayout.Live node={<Demo01 />} code={demo01SourceCode} />

You can use the `useToast` hook to add toasts. The toast's context and container
are provided by the `Toast.Context` and `Toast.Container` components. You should
not need to use them directly because they are already rendered by the
`CopyUiProvider` component.

## Dependency Graph

<DocLayout.DepGraph
  nodes={[
    { id: "toast", label: "Toast" },
    { id: "copy-ui-provider", label: "CopyUiProvider" },
    { id: "modal", label: "Modal" },
    { id: "classnames", label: "classnames", color: "green" },
    { id: "zustand", label: "zustand", color: "green" },
    { id: "es-toolkit", label: "es-toolkit", color: "green" },
  ]}
  edges={[
    { from: "toast", to: "copy-ui-provider" },
    { from: "toast", to: "modal" },
    { from: "toast", to: "classnames" },
    { from: "toast", to: "zustand" },
    { from: "toast", to: "es-toolkit" },
  ]}
/>
