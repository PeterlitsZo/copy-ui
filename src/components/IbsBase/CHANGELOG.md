- 0.1.0 (2025-12-07): Initial version.
- 0.1.1 (2025-12-16): Use `min-height` instead of `height` to let components
  built on top of it to have a flexible height.
- 0.1.2 (2026-02-12): Update the filled variant's background color.
- 0.1.3 (2026-02-13): Export `IbsBaseStyleProps` and `IbsBaseRootProps` to
  provide a stable style/root contract for components built on top of `IbsBase`.
