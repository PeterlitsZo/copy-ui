import {{ class_helper_ident }} from "{{ class_helper_pkg }}";
import type { ComponentProps, FC } from "react";

import { useJss } from "{{ components_import_base }}/CopyUiProvider";

import styles from "./table-header.module.scss";

type TableHeaderProps = ComponentProps<"thead">;

const TableHeader: FC<TableHeaderProps> = (props) => {
  const { className, ...rest } = props;

  const jss = useJss();

  const stx = jss.hash({
    "--tableHeader-borderColor": "var(--table-borderColor)",
  });

  return (
    <thead
      className={ {{- class_helper_ident -}}(styles.TableHeader, stx, className)}
      {...rest}
    />
  );
};

TableHeader.displayName = "Table.Header";

export { TableHeader };
