@layer components {
  .calendarOpener {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 90%;
    height: 90%;
    color: var(--calendarOpenerTrigger-color);
    border-radius: 999999px;
    cursor: pointer;

    &:hover {
      background-color: var(--calendarOpenerTrigger-hover-bgColor);
    }
  }
}
