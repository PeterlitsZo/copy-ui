import {{ class_helper_ident }} from "{{ class_helper_pkg }}";
import type { ComponentProps, FC } from "react";

import styles from "./modal-overlay.module.scss";

type ModalOverlayProps = ComponentProps<"div">;

const ModalOverlay: FC<ModalOverlayProps> = (props) => {
  const { className, ...rest } = props;

  return (
    <div className={ {{- class_helper_ident -}}(styles.modalOverlay, className)} {...rest} />
  );
};

ModalOverlay.displayName = "Modal.Overlay";

export { ModalOverlay };
